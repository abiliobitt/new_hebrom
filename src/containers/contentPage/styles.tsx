import styled from "styled-components";
import { SectionTitle } from "../../components/general/generalStyles";

export const ContentPageContainer = styled.div`
  padding-top: 150px;
  ${SectionTitle} {
    text-align: center;
  }
  p {
    margin: 30px 0;
  }
  img {
    width: 100%;
  }
`;
